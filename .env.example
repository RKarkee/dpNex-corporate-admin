# Upstream DpNEx API, including the version segment.
# Server-only on purpose: the browser talks to /api/proxy/*, never to the API
# directly, so this URL and the bearer token never reach client code.
API_BASE_URL=https://api.example.com/api/v1
